{
  "name": "smooth-surfer",
  "version": "0.2.3",
  "private": true,
  "description": "Surf the web with only the waves you want.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/nishu-builder/smooth-surfer.git"
  },
  "bugs": {
    "url": "https://github.com/nishu-builder/smooth-surfer/issues"
  },
  "homepage": "https://github.com/nishu-builder/smooth-surfer#readme",
  "scripts": {
    "check": "node --check src/settings.js && node --check src/storage.js && node --check src/background.js && node --check src/content.js && node --check src/popup.js && node tests/settings.test.js && node tests/manifest.test.js && node tests/background.test.js && node tests/chrome-smoke.test.mjs",
    "test": "npm run check",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{js,mjs,json}\"",
    "format:check": "prettier --check \"**/*.{js,mjs,json}\"",
    "verify": "npm run lint && npm run format:check && npm run check",
    "package": "mkdir -p dist && rm -f dist/smooth-surfer.zip && zip -r dist/smooth-surfer.zip manifest.json popup.html src icons/icon16.png icons/icon32.png icons/icon48.png icons/icon128.png",
    "screenshots": "node scripts/capture-store-assets.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6"
  }
}
