{
  "name": "feed-dock-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension with docked controls for YouTube and X/Twitter feed effects.",
  "scripts": {
    "check": "node --check src/filter-rules.js && node --check src/content.js && node tests/filter-rules.test.js && node tests/manifest.test.js"
  }
}
